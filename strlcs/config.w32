// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("strlcs", "for strlcs support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("strlcs", "enable strlcs support", "no");

if (PHP_STRLCS != "no") {
	EXTENSION("strlcs", "strlcs.c");
}

